// maps/map_kurihan.js
import { createLineA, createLineB } from "../common/tracks.js";
import { createMarkers } from "../common/markers.js";

export default async function build(viewer) {
    // ここで kurihan の線A/線B/ポイントを登録
    // 例）createLineA(viewer, { toggle: true, data: [...] });
    //     createLineB(viewer, { toggle: true, data: [...] });
    //     createMarkers(viewer, { data: [...] });
}
